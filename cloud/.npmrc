# The installed Brain and its Engine dependency both expose lifecycle hooks. Running them
# transitively creates the same skill projection twice and makes the second hook fail on EEXIST.
# Cloud installation is deliberately two-phase: dependency extraction first, then the explicit
# `prepare:runtime` script rebuilds the one native module and materializes skills exactly once.
ignore-scripts=true
